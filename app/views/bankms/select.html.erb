<h1>SelectRedirectSample</h1>

<%= form_tag bankms_path, :method => :get do %>
  <p>
    名前選択
    <%= select_tag :bank_j_select, options_for_select(Bankm.find(:all).sort_by{|bankm| (bankm.bank_cd)}.map{ |bankm| [ "#{bankm.bank_j}", bankm.bank_j]}), prompt: "名前を選択してください" , :onchange => 'submit();' %>
  </p>

<% end %>
<%= link_to '検索フォーム', root_path %>
